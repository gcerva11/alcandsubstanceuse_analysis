from data.dictionary import data, count
from classdefinitions import UsageRecord

#inside the main class lets only have the functions that were going to use for analysis
#i have the functions ive made so far for data reorganization inside the dictionary.py

#for out analysis, I think we could do

#upper vs underclassmen percentage of use frequency, and what are their implications
    #ah alright, I made seperate scripts_ignore for all the functions but I'll just write here instead.

#The dictionary would be our input then right?

def alcohol_freq(UsageRecord) -> float: #Maybe our output is a sentence with a float for percentage?


def substance_freq(UsageRecord) -> float:  # Maybe our output is a sentence with a float for percentage?

#upper vs under high_risk frequency

def high_risk_frq(UsageRecord) -> float:

#usage differences of different substances

def substance_differences(UsageRecord) -> float:

#idk what else

#and then question 22L1-12 is like about consequences, we could analys a little bit on that
#it would be a bit different tho bc it's not explicitly saying if they are upper or underclassmen

#Maybe we can show the data on students contemplating dropping out or having worse grades?
#Honestly the data wouldn't be able to show too much without suggesting connections through our own interpretation of that data
